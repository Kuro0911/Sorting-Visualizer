import styled from "styled-components";

const InsertSortWrapper = styled.div`
  text-align: center;
  .menu-wrap {
    display: flex;
    flex-direction: column;
  }
`;

export default InsertSortWrapper;
