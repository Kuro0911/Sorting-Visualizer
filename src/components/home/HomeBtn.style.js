import styled from "styled-components";

const HomeBtnWrapper = styled.div`
  color: #00adff;
  margin-left: 2em;
  margin-top: 8%;
  .icon {
    cursor: pointer;
    transform: scale(2);
  }
`;

export default HomeBtnWrapper;
