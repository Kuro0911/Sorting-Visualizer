import styled from "styled-components";

const HomeBtnWrapper = styled.div`
  .icon {
    cursor: pointer;
    color: #e779c1;
    transform: scale(2);
    margin-left: 2em;
  }
`;

export default HomeBtnWrapper;
