import styled from "styled-components";

const BubbleSortWrapper = styled.div`
  text-align: center;
  .menu-wrap {
    display: flex;
    flex-direction: column;
  }
`;

export default BubbleSortWrapper;
