import styled from "styled-components";

const QuickSortWrapper = styled.div`
  text-align: center;
  .menu-wrap {
    display: flex;
    flex-direction: column;
  }
`;

export default QuickSortWrapper;
